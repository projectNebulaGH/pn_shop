/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "export",
  basePath: "/pn_docs_next",
  images : {
    unoptimised : true
  }


};

export default nextConfig;
